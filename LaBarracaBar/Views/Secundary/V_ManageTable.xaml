<UserControl x:Class="LaBarracaBar.Views.Secundary.V_ManageTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LaBarracaBar.ViewModels"
             xmlns:ctrl="clr-namespace:LaBarracaBar.Views.Controls">
    <UserControl.DataContext>
        <vm:ManageTableViewModel/>
    </UserControl.DataContext>

    <DockPanel>
        <Button Content="Crear Mesa"
                Command="{Binding CreateTableCommand}"
                Background="#FF8C00"
                Foreground="White"
                Padding="10 5"
                BorderThickness="0"
                FontWeight="Bold"
                Height="35"
                DockPanel.Dock="Top"
                Cursor="Hand"
                Margin="0,0,0,10">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{StaticResource ButtonBackColor6}"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonBackColor5}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Width="900" Height="35"
        CornerRadius="9"
        Background="{TemplateBinding Background}" Cursor="">
                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ctrl:Table/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>