<Window x:Class="LaBarracaBar.Views.Dialogs.ChargeTableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:LaBarracaBar.Views.Controls"
        Title="Cobrar Mesa"
        WindowStartupLocation="CenterScreen"
        Height="500" Width="500"
        Background="#2E2E2E"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border>
        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
            <TextBlock Text="{Binding TableNumber}"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0 0 0 10"/>
            <!-- Detalle de productos -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False"
                  Background="#1E1E1E"
                  Foreground="White"
                  RowBackground="#252526"
                  AlternatingRowBackground="#1E1E1E"
                  BorderThickness="0"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Cant." Binding="{Binding Quantity}" Width="Auto" />
                <DataGridTextColumn Header="Precio Unit." Binding="{Binding Price, StringFormat=C}" Width="Auto" />
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="Auto" />
            </DataGrid.Columns>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="6"/>
                    <Setter Property="BorderBrush" Value="#444"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Total + Botón -->
            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0 10 0 0">
                <TextBlock Text="{Binding Total, StringFormat=Total: $ {0:F2}}"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Right"
                       Margin="0 0 0 10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar"
                            Click="OnCancel"
                            Background="#CC3300"
                            Foreground="White"
                            Padding="10 5"
                            Margin="0 0 10 0"
                            Width="90"
                            Height="35"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource ButtonBackColor6}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource ButtonBackColor5}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Width="70" Height="35"
                                        CornerRadius="9"
                                        Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Content="Facturar"
                        IsEnabled="{Binding CanFacturar}"
                        Click="OnFacturar"
                        Background="#FF8C00"
                        Foreground="White"
                        Padding="10 5"
                        Width="90"
                        Height="35"
                        BorderThickness="0"
                        Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource ButtonBackColor6}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="True">
                                        <Setter Property="Background" Value="{StaticResource ButtonBackColor5}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Width="70" Height="35"
                                            CornerRadius="9"
                                            Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>