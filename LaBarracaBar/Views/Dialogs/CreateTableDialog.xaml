<Window x:Class="LaBarracaBar.Views.Dialogs.CreateTableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:LaBarracaBar.Views.Controls"
        WindowStyle="None"
        Title="Crear Mesa" Height="400" Width="500" WindowStartupLocation="CenterScreen" Background="#2E2E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0 0 0 0">
            <TextBlock Text="Número de mesa:" Foreground="White" Margin="0,0,0,-16"/>
            <TextBox Text="{Binding TableNumber, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,220,0"
                     Width="30" Background="#333"
                     Foreground="White"
                     BorderBrush="#FF8C00"
                     BorderThickness="1"/>
            <TextBlock Text="Buscar producto:" Foreground="White" VerticalAlignment="Center" Margin="0,10,0,-16"/>
            <ComboBox Width="220"
                      Name="cbbCategory"
                      Margin="0,0,45,0"
                      ItemsSource="{Binding AvailableCategories}"
                      SelectedItem="{Binding SelectedCategoryFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Background="#333" Foreground="#333"
                      BorderBrush="#FF8C00"
                      BorderThickness="1">
                <ComboBox.Resources>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#333"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="5,2"/>
                    </Style>
                </ComboBox.Resources>
            </ComboBox>
            <Button Content="Quitar filtro"
                    Margin="0 -25 -300 10"
                    Click="OnClearFilter"
                    Background="#444"
                    Foreground="White"
                    Padding="10 5"
                    BorderThickness="0" Cursor="Hand" Width="95">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource ButtonBackColor6}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonBackColor5}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="85" Height="25"
                                CornerRadius="9"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding FilteredProductOptions}"
                  AutoGenerateColumns="False"
                  Background="#1E1E1E" Foreground="White"
                  RowBackground="#252526" AlternatingRowBackground="#1E1E1E"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding Name}" IsReadOnly="True" Width="*" />
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity, Mode=TwoWay}" Width="100"/>
            </DataGrid.Columns>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderBrush" Value="#444"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="Padding" Value="8,4"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Cancelar"
                    Click="OnCancel"
                    Background="#CC3300"
                    Foreground="White"
                    Margin="5"
                    Padding="10 5" 
                    BorderThickness="0"
                    FontWeight="Bold"
                    Width="70"
                    Height="35"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource ButtonBackColor6}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonBackColor5}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="70" Height="35"
                                CornerRadius="9"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Crear"
                    Click="OnCreate"
                    Background="#FF8C00"
                    Foreground="White"
                    Margin="5"
                    Padding="10 5"
                    BorderThickness="0"
                    FontWeight="Bold"
                    Width="70"
                    Height="35"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource ButtonBackColor6}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonBackColor5}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="70" Height="35"
                                CornerRadius="9"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>