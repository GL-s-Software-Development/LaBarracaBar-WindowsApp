<UserControl x:Class="LaBarracaBar.Views.V_Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LaBarracaBar.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.DataContext>
        <vm:HomeViewModel />
    </UserControl.DataContext>

    <Grid Background="#1E1E1E" Margin="30">
        <StackPanel>
            <TextBlock Text="Bienvenido a La Barraca Bar 🍻" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       Margin="0 0 0 20"/>

            <TextBlock Text="{Binding TodaySummary}" 
                       FontSize="16" 
                       Foreground="White" 
                       Margin="0 0 0 20"/>

            <StackPanel Orientation="Horizontal">
                <Border Background="#2E2E2E" CornerRadius="10" Padding="10" Margin="10">
                    <StackPanel>
                        <TextBlock Text="Facturación de hoy" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TodayRevenue}" FontSize="18" Foreground="#FF8C00"/>
                    </StackPanel>
                </Border>

                <Border Background="#2E2E2E" CornerRadius="10" Padding="10" Margin="10">
                    <StackPanel>
                        <TextBlock Text="Mesas activas" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ActiveTables}" FontSize="18" Foreground="#FF8C00"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
